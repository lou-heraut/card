name: tests

on:
  push:
    branches: [main]
  pull_request:

env:
  # moteur stase, installé depuis GitHub (pas de PyPI)
  STASE_GIT: "stase @ git+https://github.com/lou-heraut/stase.git"

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.11", "3.12"]
    name: pytest (py${{ matrix.python-version }})
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install
        run: |
          python -m pip install --upgrade pip
          pip install "${{ env.STASE_GIT }}"
          pip install -e .
          pip install pytest
      - name: Run tests
        run: pytest
      - name: Lint des fiches YAML (card.schema)
        run: python -m card.schema

  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
      # Version épinglée : le jeu de règles est figé dans pyproject.toml,
      # mais on épingle aussi la version pour que le comportement soit
      # exactement celui vérifié en local (cf. [tool.ruff] du pyproject).
      - run: pip install ruff==0.15.22
      - run: ruff check src tests scripts
